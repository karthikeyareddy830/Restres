/**
 * models/index.js
 *
 * Mongoose models define the data schema and interact with MongoDB.
 * Each model maps to a MongoDB collection.
 *
 * Planned models for this system:
 *   models/
 *   ├── User.model.js          → Staff, admin, customer accounts
 *   ├── Restaurant.model.js    → Restaurant profile, hours, settings
 *   ├── Table.model.js         → Table number, capacity, location
 *   └── Reservation.model.js   → Booking record, status, party details
 */

// Placeholder — models will be added with each feature module
module.exports = {};
