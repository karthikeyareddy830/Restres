/**
 * validators/index.js
 *
 * Central export point for all express-validator validation chains.
 * Individual validator files will be created here when features are added.
 *
 * Example structure (when populated):
 *   validators/
 *   ├── auth.validator.js        → email, password format validation
 *   ├── reservation.validator.js → date, party size, time slot validation
 *   └── restaurant.validator.js  → name, capacity, hours validation
 */

// Placeholder — validators will be added with each feature module
module.exports = {};
